import numpy as np
import pandas as pd
import matplotlib.pyplot as plt


# Create a Series
s = pd.Series(['Male', 'Female', 'Male', 'Female', 'Male', 'Female', 'Male'])

#Create a Series from a numpy array
s = pb.Series(n.array(['Male', 'Female', 'Male', 'Female', 'Male', 'Female', 'Male']))
